.carousel-container {
  display: flex;
  overflow-x: auto;
  overflow-y: hidden; /* спрятать вертикальную прокрутку */
  gap: 20px;
  padding: 10px 20px;
  max-width: 900px;
  margin: 0 auto 40px;
  flex-wrap: nowrap;
  align-items: center; /* горизонтальное выравнивание по центру */
  height: 60px; /* фиксированная высота */
  background-color: rgba(0, 0, 0, 0.8);
  border-radius: 12px;
  box-shadow: inset 0 0 15px #0ff;
  user-select: none;
  position: relative;
  z-index: 10;
}
