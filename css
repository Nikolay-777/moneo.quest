.carousel-container {
  display: flex;
  overflow-x: auto;
  overflow-y: hidden; /* Передает только горизонтальную прокрутку */
  gap: 20px;
  padding: 10px 20px;
  max-width: 900px;
  margin: 0 auto 40px;
  flex-wrap: nowrap;
  align-items: center; /* Выравнивание по вертикали */
  height: 60px; /* Фиксированная высота для контейнера */
  background-color: rgba(0,0,0,0.8); /* Темный фон, можно менять */
  border-radius: 12px;
  box-shadow: inset 0 0 15px #0ff;
}

.carousel-button {
  height: 44px; /* Фиксированная высота кнопок для выравнивания */
  display: flex;
  align-items: center;
  justify-content: center;
  min-width: 140px;
  padding: 0 20px;
  border-radius: 20px;
  background: rgba(0,255,255,0.15);
  color: #0ff;
  font-weight: 700;
  font-size: 1rem;
  white-space: nowrap;
  cursor: pointer;
  user-select: none;
  transition: all 0.3s ease;
}

.carousel-button:hover,
.carousel-button:focus {
  background-color: #0ff;
  color: #003f3f;
  box-shadow: 0 0 15px #0ff, inset 0 0 20px #66ffff;
  outline: none;
}
